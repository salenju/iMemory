import React, { useState, useEffect } from "react";
import styled from "styled-components";

const Register = () => {
  return <Wrapper className="register">Register Page</Wrapper>;
};

const Wrapper = styled("div")``;

export default Register;
