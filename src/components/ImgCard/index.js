import React from 'react'
import styled from 'styled-components'

/**
 *
 * @param {*} props
 * @param {*} props
 * @param {*} props
 */

const ImgCard = (props) => {
  return <Wrapper className="">ImgCard</Wrapper>
}

const Wrapper = styled('div')``

export default ImgCard
